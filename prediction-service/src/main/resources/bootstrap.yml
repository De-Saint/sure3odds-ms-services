server:
  port: 8000


spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sure3odds_db_prediction_service?useTimezone=true&serverTimezone=Africa/Lagos
  application:
    name: prediction-service
